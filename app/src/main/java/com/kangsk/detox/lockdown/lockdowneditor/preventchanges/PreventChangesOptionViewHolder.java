package com.kangsk.detox.lockdown.lockdowneditor.preventchanges;

import android.view.View;

import androidx.recyclerview.widget.RecyclerView;

public class PreventChangesOptionViewHolder extends RecyclerView.ViewHolder {

    /*
     * CONSTRUCTOR: responsible for injecting and instantiating fields.
     */
    public PreventChangesOptionViewHolder(View itemView) {
        super(itemView);
    }

    /*
     * bindModel: method responsible for retrieving the necessary data and supplying it
     * to the appropriate Views.
     */
    public void bindModel() {

    }
}
